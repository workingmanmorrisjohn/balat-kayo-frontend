export enum RoutePath {
    HOME = "/",
    ROOM_CODE = "/room_code",
    LOBBY = "/lobby",
    EDIT_APPEARANCE = "/edit-appearance",
    COUNTDOWN = "/countdown",
    GAMEPLAY = "/gameplay",
    RESULTS = "/results",
    VOTING = "/voting"
}